export interface WritingItem {
  title: string;
  url: string;
  date: string;
  description: string;
}

// External articles shown on /writing alongside local posts in content/writing.
// Empty for now, ready for the first entry.
const data: WritingItem[] = [];

export default data;
